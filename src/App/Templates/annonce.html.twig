{% extends 'base.html.twig' %}

{% block body %}
    <div class="single__annonce">

        <div class="all__images">
            <div class="row">
                <div class="col-md-9">
                    <div class="row">
                        {% for img in  annonce.images %}
                            <div class="col-md-4">
                                <img src="{{ img.url }}" alt="">
                            </div>
                        {% endfor %}
                    </div>
                    <div class="title">{{ annonce.title }}</div>
                    <div class="price">
                        {{ annonce.price }}
                    </div>
                    <div class="date">
                        <h6 class="h5 card-title">Catégorie : </h6>
                        {% for catk in annonce.category %}
                            <p> -> {{ catk.name }}</p>
                        {% endfor %}
                    </div>

                    <div class="category">

                    </div>
                    <h2>Description</h2>
                    <p class="description">{{ annonce.content }}</p>
                </div>
                <div class="col-md-3">

                </div>
            </div>

        </div>

    </div>
{% endblock %}
